# 物理验证凭证 (Proof of Work)

## 1. 验证目标
[例如：验证登录接口能否正常返回 JWT]

## 2. 执行探针
`[填写具体命令，如：curl -X POST http://localhost/... ]`

## 3. 执行结果
```text
[粘贴原始 stdout/stderr 输出，必须包含 Exit Code]
```

## 4. 结论
- [ ] 通过
- [ ] 不通过（需退回 Execute 阶段）
