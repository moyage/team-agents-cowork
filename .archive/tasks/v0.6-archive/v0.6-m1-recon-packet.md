# V0.6-M1 Reconnaissance: Repository Hygiene

## 1. Objective
Establish repository hygiene by executing a global purge of `.DS_Store` files, ensuring they are ignored in `.gitignore`, and performing the lowest-risk repository noise reduction.

## 2. Current State Analysis
- **Target Files:** macOS `.DS_Store` files are scattered across the repository (e.g., `./`, `examples/`, `retros/`, `src/`, `tasks/`).
- **`.gitignore` Status:** Need to verify if `.DS_Store` is correctly and globally ignored to prevent future pollution.
- **Risk Assessment:** Deleting `.DS_Store` files carries zero architectural or operational risk. It does not affect execution contracts, workflow state, or the MCP Server functionality.

## 3. Scope Definition
- **In-Scope:** Finding and removing all `.DS_Store` files in the repository. Verifying and updating `.gitignore` to explicitly exclude `.DS_Store`.
- **Out-of-Scope:** Archiving historical data, rewriting documentation, refactoring MCP server code, or modifying workflow/state files.

## 4. Expected Artifacts
- `tasks/v0.6-m1-execution-contract.json`
- `tasks/v0.6-m1-contract-review-decision.json`
- `tasks/v0.6-m1-execution-result.json`
- `tasks/v0.6-m1-result-review-packet.json`
- `tasks/v0.6-m1-result-review-decision.json`
