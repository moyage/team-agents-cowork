# Quick Start

Initialize the protocol in your repository:

```bash
npx team-agents-cowork-init
# or
node bin/agent-protocol-init
```

This injects `.cursorrules`, `CLAUDE.md`, and creates the base `workflow/` directory.

Once initialized, prompt your AI assistant to read `documentation/EN/SYSTEM_PROMPT.md` to begin generating contracts and executing workflows.
