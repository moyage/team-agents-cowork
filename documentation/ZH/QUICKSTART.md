# 快速开始

在你的目标代码库中初始化协议：

```bash
npx team-agents-cowork-init
# 或
node bin/agent-protocol-init
```

这会自动注入 `.cursorrules`、`CLAUDE.md` 等 IDE 规则，并创建基础的 `workflow/` 目录。

初始化后，只需让你的 AI 助手读取 `documentation/ZH/SYSTEM_PROMPT.md`，它就会自动开始按照协议生成合约和流转任务。
