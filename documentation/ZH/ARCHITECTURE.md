# 架构说明

## L2 / L3 分层隔离
- **L2 (执行层):** 实际生成代码的活动 AI。容易受到大模型幻觉的影响。
- **L3 (治理层):** MCP Server 和 Gatekeeper 协议。它是不可变的、物理隔离的，且带有对抗审查性质的网关。

## 状态抽象
系统目前正在从内部挂载状态 (`tasks/`, `workflow/`) 向外部用户代码库注入 `.agent-state/` 文件夹转变，使得 `team-agents-cowork` 彻底退化为一个无状态的 NPM 执行引擎。
