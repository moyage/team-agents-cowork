# Minimal Integration Example
This dummy directory demonstrates how a target project receives the Team Agent Collaboration Protocol protocol.
Run `../scripts/sync_to_target.py ./` to see `AGENTS.md` and `.cursorrules` injected here.\n